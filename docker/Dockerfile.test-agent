FROM ubuntu:22.04

RUN apt-get update && apt-get install -y ca-certificates && rm -rf /var/lib/apt/lists/*

COPY target/x86_64-unknown-linux-gnu/release/workspace-agent /usr/local/bin/workspace-agent

ENV RUST_LOG=info

CMD ["/usr/local/bin/workspace-agent"]
