# Elevo Workspace 生产环境配置
# 修改此文件后执行 ./deploy.sh restart 生效

# ============================================================================
# 服务端口
# ============================================================================
HTTP_PORT=8080
GRPC_PORT=9090
NFS_PORT=2049

# NFS 外部访问地址 (用于返回给客户端的 nfs_url)
# 如果不设置，默认为 127.0.0.1
# NFS_HOST=your-server-ip

# ============================================================================
# 存储配置
# ============================================================================
# 工作空间目录 (宿主机路径，sandbox 容器会挂载此目录)
WORKSPACE_HOST_DIR=/var/lib/elevo-workspace/workspaces

# ============================================================================
# 镜像配置
# ============================================================================
SERVER_IMAGE=ghcr.io/openelevo/elevosandbox-server:latest
BASE_IMAGE=ghcr.io/openelevo/elevosandbox-base:latest

# ============================================================================
# MCP 配置
# ============================================================================
# MCP 模式: disabled, http
#   - disabled: 不启用 MCP
#   - http: 启用 HTTP/SSE 方式的 MCP 服务
MCP_MODE=http

# MCP 端点路径前缀
# 启用后提供三个端点:
#   - {MCP_PATH}/executor   (1 tool: process_run)
#   - {MCP_PATH}/developer  (6 tools: process + file)
#   - {MCP_PATH}/full       (14 tools: all)
MCP_PATH=/mcp

# ============================================================================
# 高级配置 (一般不需要修改)
# ============================================================================
# Agent 连接超时 (秒)
AGENT_TIMEOUT=60

# Sandbox 最大空闲时间 (秒)，超时自动清理
MAX_IDLE_TIME=7200

# 日志级别
LOG_LEVEL=info

# 容器名称
CONTAINER_NAME=elevo-workspace-server
